# Variables d'environnement — backend Express + SQLite (v2)

# Chemin vers la base de données SQLite (dans le volume Docker)
DATABASE_PATH=/usr/src/app/data/loan-calc.db

# Port d'écoute du serveur Express
API_PORT=3000
